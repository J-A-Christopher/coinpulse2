class IncomeModel {
  String id;
  String title;
  String amount;
  DateTime createdDate;
  IncomeModel(
      {required this.amount,
      required this.createdDate,
      required this.title,
      required this.id});
}
